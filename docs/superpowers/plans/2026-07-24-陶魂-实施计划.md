# 「陶魂」荣昌陶器节奏游戏 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一款写实质感风格的静态网页游戏，玩家依次体验练泥→拉坯→修坯→刻花→烧制五个荣昌陶器工艺环节，每个环节为节奏/时机玩法，最终获得评分。

**Architecture:** 单页 HTML + Canvas 2D 渲染 + 全局命名空间 `TaoHun`。所有 JS 通过 `<script>` 标签按依赖顺序加载，兼容 `file://` 本地打开。游戏状态机驱动流程切换，每个环节实现统一 Stage 接口。

**Tech Stack:** HTML5, CSS3, Vanilla JS (ES6), Canvas 2D API, Web Audio API (OscillatorNode), Pointer Events

## Global Constraints

- 纯前端，无框架，无外部依赖
- `file://` 协议可本地打开运行
- 兼容 Chrome / Edge / Firefox / Safari 近两年版本
- 触屏 + 鼠标统一通过 Pointer Events 处理
- 写实质感视觉风格：深色背景 `#1a0c04`，陶土色系 `#8b5a30` ~ `#c49470`，火焰暖光 `#ff9940`
- 不使用任何外部图片/字体/音频文件，全部程序化生成

## File Structure

```
/
├── index.html              # 入口 + 加载顺序
├── css/
│   └── style.css           # 全局样式
└── js/
    ├── main.js             # 游戏主控：状态机、流程调度、分数管理
    ├── ui.js               # UI 组件：HUD、过渡动画、评分面板、菜单
    ├── particles.js        # 粒子系统：气泡、泥屑、火星
    ├── audio.js            # 音效合成（Web Audio API）
    └── stages/
        ├── knead.js        # ① 练泥 —「揉泥去浊」
        ├── throw.js        # ② 拉坯 —「轮转成形」
        ├── trim.js         # ③ 修坯 —「刀走泥落」
        ├── carve.js        # ④ 刻花 —「铁笔生花」
        └── fire.js         # ⑤ 烧制 —「烈火成器」
```

## Interfaces

每个 Stage 模块必须实现以下接口（挂载到 `TaoHun.Stages.Xxx`）：

```js
{
  init(ctx, canvas)  // 初始化，接收 Canvas 2D 上下文和元素
  start()            // 环节开始，重置状态，返回 void
  update(dt)         // 每帧更新，dt 为秒数
  render(ctx)        // 每帧绘制
  handleInput(e)     // 处理 PointerEvent
  isComplete()       // 返回 bool，环节是否结束
  getScore()         // 返回 { score: 0-100, details: {...} }
  cleanup()          // 清理资源
}
```

---

### Task 1: 项目骨架 — HTML + CSS + Canvas 舞台

**Files:**
- Create: `index.html`
- Create: `css/style.css`

**Produces:** 可打开的游戏空白页，Canvas 充满屏幕，基础 CSS 变量和噪点纹理就位。

- [ ] **Step 1: 创建 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>陶魂 — 荣昌陶器</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <canvas id="gameCanvas"></canvas>
  <div id="uiLayer"></div>

  <!-- 依赖顺序：底层 → 上层 -->
  <script src="js/audio.js"></script>
  <script src="js/particles.js"></script>
  <script src="js/ui.js"></script>
  <script src="js/stages/knead.js"></script>
  <script src="js/stages/throw.js"></script>
  <script src="js/stages/trim.js"></script>
  <script src="js/stages/carve.js"></script>
  <script src="js/stages/fire.js"></script>
  <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: 创建 CSS 全局样式**

```css
/* === 写实质感风全局样式 === */
:root {
  --bg-deep: #1a0c04;
  --bg-kiln: #2a1808;
  --clay-dark: #6b3a1a;
  --clay-mid: #8b5a30;
  --clay-light: #c49470;
  --clay-pale: #e8c8a0;
  --fire-bright: #ff9940;
  --fire-warm: #ffcc66;
  --fire-hot: #ff6b35;
  --glaze-green: #7ab8a0;
  --glaze-brown: #6b3a2a;
  --glaze-red: #c44;
  --text-light: #e8c8a0;
  --text-pale: #f5e6d0;
  --ui-font: 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif;
  --title-font: 'Georgia', 'SimSun', 'KaiTi', serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html, body {
  width: 100%; height: 100%;
  overflow: hidden;
  background: var(--bg-deep);
  font-family: var(--ui-font);
  /* 噪点纹理 */
  position: relative;
}

body::before {
  content: '';
  position: fixed; inset: 0;
  opacity: 0.04;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
  background-size: 256px 256px;
  pointer-events: none; z-index: 999;
}

#gameCanvas {
  position: absolute; top: 0; left: 0;
  width: 100%; height: 100%;
}

#uiLayer {
  position: absolute; top: 0; left: 0;
  width: 100%; height: 100%;
  pointer-events: none; z-index: 10;
}

#uiLayer > * { pointer-events: auto; }

/* 按钮共用样式 */
.btn {
  display: inline-block; padding: 12px 36px;
  border: 2px solid var(--fire-bright);
  background: rgba(255, 153, 64, 0.1);
  color: var(--fire-bright);
  font-family: var(--title-font);
  font-size: 18px; letter-spacing: 4px;
  cursor: pointer; border-radius: 4px;
  transition: all 0.3s;
  text-align: center; user-select: none;
}
.btn:hover { background: rgba(255, 153, 64, 0.25); }
.btn:active { background: rgba(255, 153, 64, 0.4); }

/* 标题 */
.game-title {
  font-family: var(--title-font);
  color: var(--text-pale);
  font-size: 48px; letter-spacing: 8px;
  text-shadow: 0 0 40px rgba(255, 150, 50, 0.3);
}

.game-subtitle {
  font-family: var(--ui-font);
  color: var(--clay-pale);
  font-size: 16px; letter-spacing: 6px;
  margin-top: 12px;
}
```

- [ ] **Step 3: 创建 js/main.js 空壳**

```js
// 全局命名空间
window.TaoHun = window.TaoHun || {};

TaoHun.Game = {
  canvas: null,
  ctx: null,
  state: 'menu',    // menu | playing | transition | result
  currentStage: 0,
  scores: [0, 0, 0, 0, 0],
  stageInstances: [],
  stageNames: ['练泥', '拉坯', '修坯', '刻花', '烧制'],
  stageKeys: ['knead', 'throw', 'trim', 'carve', 'fire'],

  init() {
    this.canvas = document.getElementById('gameCanvas');
    this.ctx = this.canvas.getContext('2d');
    this.resize();
    window.addEventListener('resize', () => this.resize());
    this.loop(0);
    this.showMenu();
  },

  resize() {
    this.canvas.width = window.innerWidth * devicePixelRatio;
    this.canvas.height = window.innerHeight * devicePixelRatio;
    this.canvas.style.width = window.innerWidth + 'px';
    this.canvas.style.height = window.innerHeight + 'px';
    this.ctx.setTransform(1, 0, 0, 1, 0, 0);
    this.ctx.scale(devicePixelRatio, devicePixelRatio);
  },

  loop(timestamp) {
    requestAnimationFrame(t => this.loop(t));
    const dt = Math.min((timestamp - (this._lastTime || timestamp)) / 1000, 0.1);
    this._lastTime = timestamp;
    if (this.state === 'playing' && this.currentInstance) {
      this.currentInstance.update(dt);
    }
    this.render();
  },

  render() {
    const ctx = this.ctx;
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    // 背景
    ctx.fillStyle = '#1a0c04';
    ctx.fillRect(0, 0, w, h);

    if (this.state === 'playing' && this.currentInstance) {
      this.currentInstance.render(ctx);
    }
    if (this.state === 'playing') {
      TaoHun.UI.renderHUD(ctx, this.currentStage, this.scores[this.currentStage]);
    }
  },

  showMenu() {
    this.state = 'menu';
    TaoHun.UI.showMenu((shape) => {
      this.selectedShape = shape;
      this.startGame();
    });
  },

  startGame() {
    this.currentStage = 0;
    this.scores = [0, 0, 0, 0, 0];
    this.startStage();
  },

  startStage() {
    const key = this.stageKeys[this.currentStage];
    const stageModule = TaoHun.Stages[key.charAt(0).toUpperCase() + key.slice(1)];
    this.currentInstance = Object.create(stageModule);
    this.currentInstance.init(this.ctx, this.canvas);
    this.currentInstance.start();
    this.state = 'playing';
    TaoHun.UI.showStageIntro(this.stageNames[this.currentStage], () => {});
  },

  onStageComplete() {
    const result = this.currentInstance.getScore();
    this.scores[this.currentStage] = result.score;
    this.currentInstance.cleanup();
    this.currentInstance = null;

    this.state = 'transition';
    this.currentStage++;
    if (this.currentStage >= 5) {
      this.showResult();
    } else {
      TaoHun.UI.showTransition(() => this.startStage());
    }
  },

  showResult() {
    this.state = 'result';
    TaoHun.UI.showResult(this.scores, () => this.showMenu());
  },

  handleInput(e) {
    if (this.state === 'playing' && this.currentInstance) {
      this.currentInstance.handleInput(e);
    }
  }
};

// 全局 pointer 事件
document.addEventListener('pointerdown', e => TaoHun.Game.handleInput(e));
document.addEventListener('pointermove', e => TaoHun.Game.handleInput(e));
document.addEventListener('pointerup', e => TaoHun.Game.handleInput(e));
document.addEventListener('keydown', e => TaoHun.Game.handleInput(e));
document.addEventListener('keyup', e => TaoHun.Game.handleInput(e));

// 启动
window.addEventListener('DOMContentLoaded', () => TaoHun.Game.init());
```

- [ ] **Step 4: 创建空模块文件**

为 `js/audio.js`, `js/particles.js`, `js/ui.js` 和 `js/stages/` 下五个文件各创建最小命名空间声明：

```js
// 每个文件内容示例：
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};
```

- [ ] **Step 5: 验证** — 在浏览器中打开 `index.html`，确认：Canvas 显示深色背景，console 无报错，`TaoHun.Game` 全局可访问。

---

### Task 2: 音效系统 — Web Audio API 合成

**Files:**
- Modify: `js/audio.js`

**Produces:** `TaoHun.Audio` 模块，提供 `play(name)` 方法，合成游戏所需全部音效。

- [ ] **Step 1: 实现 audio.js**

```js
window.TaoHun = window.TaoHun || {};

TaoHun.Audio = {
  ctx: null,
  _initialized: false,

  init() {
    if (this._initialized) return;
    try {
      this.ctx = new (window.AudioContext || window.webkitAudioContext)();
      this._initialized = true;
    } catch (e) {
      console.warn('Web Audio API not available');
    }
  },

  _ensure() {
    if (!this._initialized) this.init();
    if (this.ctx && this.ctx.state === 'suspended') this.ctx.resume();
  },

  play(name) {
    this._ensure();
    if (!this.ctx) return;
    const fn = this._sounds[name];
    if (fn) fn(this.ctx);
  },

  _sounds: {
    // 戳气泡 — 短促清脆
    pop(ctx) {
      const t = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'sine';
      osc.frequency.setValueAtTime(800, t);
      osc.frequency.exponentialRampToValueAtTime(200, t + 0.08);
      gain.gain.setValueAtTime(0.15, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.08);
      osc.connect(gain).connect(ctx.destination);
      osc.start(t); osc.stop(t + 0.08);
    },

    // 完美时机 — 叮
    perfect(ctx) {
      const t = ctx.currentTime;
      [880, 1100].forEach((freq, i) => {
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.type = 'triangle';
        osc.frequency.value = freq;
        gain.gain.setValueAtTime(0.12, t + i * 0.06);
        gain.gain.exponentialRampToValueAtTime(0.001, t + i * 0.06 + 0.15);
        osc.connect(gain).connect(ctx.destination);
        osc.start(t + i * 0.06); osc.stop(t + i * 0.06 + 0.15);
      });
    },

    // 一般时机 — 嗒
    good(ctx) {
      const t = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'sine';
      osc.frequency.value = 440;
      gain.gain.setValueAtTime(0.1, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.1);
      osc.connect(gain).connect(ctx.destination);
      osc.start(t); osc.stop(t + 0.1);
    },

    // 失误 — 低沉闷响
    miss(ctx) {
      const t = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'sawtooth';
      osc.frequency.setValueAtTime(120, t);
      osc.frequency.exponentialRampToValueAtTime(60, t + 0.2);
      gain.gain.setValueAtTime(0.1, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.2);
      osc.connect(gain).connect(ctx.destination);
      osc.start(t); osc.stop(t + 0.2);
    },

    // 削泥 — 沙沙声
    scrape(ctx) {
      const t = ctx.currentTime;
      const bufferSize = ctx.sampleRate * 0.1;
      const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
      const data = buffer.getChannelData(0);
      for (let i = 0; i < bufferSize; i++) {
        data[i] = (Math.random() * 2 - 1) * (1 - i / bufferSize);
      }
      const src = ctx.createBufferSource();
      const gain = ctx.createGain();
      const filter = ctx.createBiquadFilter();
      filter.type = 'bandpass'; filter.frequency.value = 3000; filter.Q.value = 0.5;
      src.buffer = buffer;
      gain.gain.setValueAtTime(0.08, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.1);
      src.connect(filter).connect(gain).connect(ctx.destination);
      src.start(t); src.stop(t + 0.1);
    },

    // 刻花 — 尖锐轻音
    scratch(ctx) {
      const t = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'triangle';
      osc.frequency.setValueAtTime(2000, t);
      osc.frequency.exponentialRampToValueAtTime(600, t + 0.05);
      gain.gain.setValueAtTime(0.04, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.05);
      osc.connect(gain).connect(ctx.destination);
      osc.start(t); osc.stop(t + 0.05);
    },

    // 加柴 — 火焰噼啪
    fireCrackle(ctx) {
      const t = ctx.currentTime;
      const bufferSize = ctx.sampleRate * 0.15;
      const buffer = ctx.createBuffer(1, bufferSize, ctx.sampleRate);
      const data = buffer.getChannelData(0);
      for (let i = 0; i < bufferSize; i++) {
        const envelope = Math.sin(Math.PI * i / bufferSize);
        data[i] = (Math.random() * 2 - 1) * envelope * 0.8;
      }
      const src = ctx.createBufferSource();
      const gain = ctx.createGain();
      const filter = ctx.createBiquadFilter();
      filter.type = 'bandpass'; filter.frequency.value = 800; filter.Q.value = 0.3;
      src.buffer = buffer;
      gain.gain.setValueAtTime(0.12, t);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.15);
      src.connect(filter).connect(gain).connect(ctx.destination);
      src.start(t); src.stop(t + 0.15);
    },

    // 环节切换 — 古韵钟声
    transition(ctx) {
      const t = ctx.currentTime;
      [392, 523, 659].forEach((freq, i) => {
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.type = 'sine';
        osc.frequency.value = freq;
        gain.gain.setValueAtTime(0, t + i * 0.2);
        gain.gain.linearRampToValueAtTime(0.12, t + i * 0.2 + 0.05);
        gain.gain.exponentialRampToValueAtTime(0.001, t + i * 0.2 + 0.6);
        osc.connect(gain).connect(ctx.destination);
        osc.start(t + i * 0.2); osc.stop(t + i * 0.2 + 0.6);
      });
    },

    // 完成 — 胜利
    complete(ctx) {
      const t = ctx.currentTime;
      [523, 659, 784, 1047].forEach((freq, i) => {
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.type = 'triangle';
        osc.frequency.value = freq;
        gain.gain.setValueAtTime(0, t + i * 0.15);
        gain.gain.linearRampToValueAtTime(0.1, t + i * 0.15 + 0.05);
        gain.gain.exponentialRampToValueAtTime(0.001, t + i * 0.15 + 0.5);
        osc.connect(gain).connect(ctx.destination);
        osc.start(t + i * 0.15); osc.stop(t + i * 0.15 + 0.5);
      });
    }
  }
};
```

- [ ] **Step 2: 验证** — 在浏览器 console 执行 `TaoHun.Audio.init(); TaoHun.Audio.play('pop');`，应听到短促音效。

---

### Task 3: 粒子系统

**Files:**
- Modify: `js/particles.js`

**Produces:** `TaoHun.Particles` 模块，支持气泡、泥屑、火星三种粒子发射器。

- [ ] **Step 1: 实现 particles.js**

```js
window.TaoHun = window.TaoHun || {};

TaoHun.Particles = {
  emitters: [],

  // config: { x, y, type: 'bubble'|'chip'|'spark', count, spread, speed, life, size }
  emit(config) {
    const particles = [];
    for (let i = 0; i < (config.count || 10); i++) {
      const angle = (Math.PI * 2 / config.count) * i + (Math.random() - 0.5) * (config.spread || 1);
      const speed = (config.speed || 100) * (0.5 + Math.random());
      particles.push({
        type: config.type || 'chip',
        x: config.x, y: config.y,
        vx: Math.cos(angle) * speed,
        vy: Math.sin(angle) * speed - (config.type === 'spark' ? speed * 0.3 : 0),
        life: config.life || 0.6,
        maxLife: config.life || 0.6,
        size: config.size || (2 + Math.random() * 3),
        color: config.color || '#c49470',
      });
    }
    this.emitters.push({ particles, age: 0 });
  },

  update(dt) {
    for (const emitter of this.emitters) {
      emitter.age += dt;
      for (const p of emitter.particles) {
        p.x += p.vx * dt;
        p.y += p.vy * dt;
        p.life -= dt;
        if (p.type === 'spark') p.vy -= 80 * dt; // 火星上升
        if (p.type === 'chip') p.vy += 200 * dt; // 泥屑下落
      }
    }
    // 清理过期粒子
    this.emitters = this.emitters.filter(e => e.age < 2);
  },

  render(ctx) {
    for (const emitter of this.emitters) {
      for (const p of emitter.particles) {
        if (p.life <= 0) continue;
        const alpha = Math.max(0, p.life / p.maxLife);
        ctx.save();
        ctx.globalAlpha = alpha;
        ctx.fillStyle = p.color;
        if (p.type === 'spark') {
          // 火星：小圆点 + 光晕
          const grad = ctx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.size * 2);
          grad.addColorStop(0, p.color);
          grad.addColorStop(1, 'transparent');
          ctx.fillStyle = grad;
          ctx.beginPath(); ctx.arc(p.x, p.y, p.size * 2, 0, Math.PI * 2); ctx.fill();
        } else if (p.type === 'bubble') {
          // 气泡：半透明白色圆
          const grad = ctx.createRadialGradient(p.x - 1, p.y - 1, 0, p.x, p.y, p.size);
          grad.addColorStop(0, 'rgba(255,255,255,0.6)');
          grad.addColorStop(0.6, 'rgba(200,180,150,0.3)');
          grad.addColorStop(1, 'rgba(200,180,150,0)');
          ctx.fillStyle = grad;
          ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2); ctx.fill();
        } else {
          // 泥屑：不规则小点
          ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2); ctx.fill();
        }
        ctx.restore();
      }
    }
  },

  clear() {
    this.emitters = [];
  }
};
```

- [ ] **Step 2: 验证** — 在 console 执行 `TaoHun.Particles.emit({x:400,y:300,type:'spark',count:20,spread:3,speed:150,life:0.8,size:3,color:'#ff9940'});` 确认粒子在主循环中渲染。

---

### Task 4: UI 系统

**Files:**
- Modify: `js/ui.js`

**Produces:** `TaoHun.UI` 模块，提供菜单、环节导语、HUD、过渡动画、结果面板。

- [ ] **Step 1: 实现 ui.js 核心方法**

```js
window.TaoHun = window.TaoHun || {};

TaoHun.UI = {
  // === 主菜单 ===
  showMenu(onStart) {
    const ui = document.getElementById('uiLayer');
    ui.innerHTML = `
      <div class="menu-container" style="
        position:absolute;inset:0;display:flex;flex-direction:column;
        align-items:center;justify-content:center;gap:24px;
      ">
        <div class="game-title" style="margin-bottom:8px;">陶 魂</div>
        <div class="game-subtitle">荣昌陶器 · 节奏技艺之旅</div>
        <div style="margin-top:20px;display:flex;gap:16px;flex-wrap:wrap;justify-content:center;">
          ${['碗','罐','瓶','坛'].map(s => `
            <div class="shape-card" data-shape="${s}" style="
              width:100px;height:100px;border:2px solid #5a3a1a;
              border-radius:50%;display:flex;align-items:center;justify-content:center;
              cursor:pointer;background:rgba(139,90,48,0.15);
              transition:all 0.3s;color:var(--clay-pale);font-size:20px;
              letter-spacing:2px;
            ">${s}</div>
          `).join('')}
        </div>
        <p style="color:var(--clay-mid);font-size:13px;margin-top:8px;">选择器型开始制陶</p>
      </div>
    `;
    // 绑定事件
    ui.querySelectorAll('.shape-card').forEach(card => {
      card.addEventListener('pointerenter', () => {
        card.style.borderColor = 'var(--fire-bright)';
        card.style.background = 'rgba(255,153,64,0.2)';
      });
      card.addEventListener('pointerleave', () => {
        card.style.borderColor = '#5a3a1a';
        card.style.background = 'rgba(139,90,48,0.15)';
      });
      card.addEventListener('pointerdown', (e) => {
        e.stopPropagation();
        const shape = card.dataset.shape;
        ui.innerHTML = '';
        TaoHun.Audio.play('transition');
        onStart(shape);
      });
    });
  },

  // === 环节导语 ===
  showStageIntro(name, onDone) {
    const ui = document.getElementById('uiLayer');
    const titles = {
      '练泥': ['揉泥去浊', '排除泥中气泡，使泥料均匀'],
      '拉坯': ['轮转成形', '把握提拉时机，塑造器型'],
      '修坯': ['刀走泥落', '削去多余泥料，修整器壁'],
      '刻花': ['铁笔生花', '沿纹样刻画，赋予器物灵魂'],
      '烧制': ['烈火成器', '掌控窑温，淬炼出釉色'],
    };
    const [sub, hint] = titles[name];
    ui.innerHTML = `
      <div class="intro-overlay" style="
        position:absolute;inset:0;display:flex;flex-direction:column;
        align-items:center;justify-content:center;
        background:rgba(26,12,4,0.85);z-index:20;
      ">
        <div style="font-family:var(--title-font);font-size:36px;color:var(--text-pale);
          letter-spacing:6px;text-shadow:0 0 30px rgba(255,150,50,0.3);">${name}</div>
        <div style="font-family:var(--title-font);font-size:22px;color:var(--fire-bright);
          letter-spacing:8px;margin-top:12px;">${sub}</div>
        <div style="color:var(--clay-mid);font-size:14px;margin-top:16px;letter-spacing:2px;">${hint}</div>
      </div>
    `;
    setTimeout(() => {
      ui.innerHTML = '';
      if (onDone) onDone();
      // 初始化 Audio context（需要用户交互）
      TaoHun.Audio.init();
    }, 2000);
  },

  // === 游戏 HUD ===
  renderHUD(ctx, stageIndex, score) {
    const names = ['练泥', '拉坯', '修坯', '刻花', '烧制'];
    // 进度条
    const barW = 300, barH = 4, barX = (ctx.canvas.width / devicePixelRatio - barW) / 2, barY = 30;
    ctx.fillStyle = 'rgba(255,255,255,0.1)';
    ctx.fillRect(barX, barY, barW, barH);
    const progress = (stageIndex + (score > 0 ? 0.5 : 0)) / 5;
    const grad = ctx.createLinearGradient(barX, 0, barX + barW, 0);
    grad.addColorStop(0, '#ff6b35'); grad.addColorStop(1, '#ff9940');
    ctx.fillStyle = grad;
    ctx.fillRect(barX, barY, barW * progress, barH);

    // 环节名
    ctx.fillStyle = '#e8c8a0';
    ctx.font = '14px "PingFang SC","Microsoft YaHei",sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(`${names[stageIndex]} · 得分 ${Math.round(score)}`, barX + barW / 2, barY - 10);

    // 环节指示器
    const dotY = barY + 20, dotSpacing = barW / 4;
    for (let i = 0; i < 5; i++) {
      ctx.beginPath();
      ctx.arc(barX + dotSpacing * i, dotY, 4, 0, Math.PI * 2);
      ctx.fillStyle = i < stageIndex ? '#ff9940' : i === stageIndex ? '#e8c8a0' : 'rgba(255,255,255,0.2)';
      ctx.fill();
      ctx.fillStyle = '#8b5a30';
      ctx.font = '10px "PingFang SC","Microsoft YaHei",sans-serif';
      ctx.textAlign = 'center';
      ctx.fillText(names[i].charAt(0), barX + dotSpacing * i, dotY + 18);
    }
  },

  // === 环节过渡 ===
  showTransition(onDone) {
    TaoHun.Audio.play('transition');
    const ui = document.getElementById('uiLayer');
    ui.innerHTML = `
      <div class="transition-overlay" style="
        position:absolute;inset:0;display:flex;align-items:center;justify-content:center;
        background:rgba(26,12,4,0.7);z-index:20;
      ">
        <div style="color:var(--fire-bright);font-size:24px;letter-spacing:6px;
          animation: pulse 0.8s ease-in-out;">
          下一环节...
        </div>
      </div>
      <style>
        @keyframes pulse { 0%,100%{opacity:0.3;} 50%{opacity:1;} }
      </style>
    `;
    setTimeout(() => { ui.innerHTML = ''; onDone(); }, 1500);
  },

  // === 最终结果 ===
  showResult(scores, onRestart) {
    TaoHun.Audio.play('complete');
    const total = Math.round(scores.reduce((a, b) => a + b, 0) / scores.length);
    let rank, rankColor;
    if (total >= 95) { rank = '神品'; rankColor = '#ffcc00'; }
    else if (total >= 85) { rank = '精品'; rankColor = '#ff9940'; }
    else if (total >= 70) { rank = '良品'; rankColor = '#7ab8a0'; }
    else { rank = '习作'; rankColor = '#8b5a30'; }

    const names = ['练泥', '拉坯', '修坯', '刻花', '烧制'];
    const scoreRows = names.map((n, i) => `
      <div style="display:flex;justify-content:space-between;width:240px;
        padding:6px 0;border-bottom:1px solid rgba(255,255,255,0.05);">
        <span style="color:#c49470;">${n}</span>
        <span style="color:#e8c8a0;font-weight:bold;">${scores[i]}</span>
      </div>
    `).join('');

    const ui = document.getElementById('uiLayer');
    ui.innerHTML = `
      <div class="result-overlay" style="
        position:absolute;inset:0;display:flex;flex-direction:column;
        align-items:center;justify-content:center;gap:16px;
        background:rgba(26,12,4,0.9);z-index:20;
      ">
        <div class="game-title" style="font-size:28px;">陶成</div>
        <div style="font-size:56px;color:${rankColor};letter-spacing:8px;
          text-shadow:0 0 40px ${rankColor}44;">${rank}</div>
        <div style="font-size:18px;color:var(--clay-pale);">总评 ${total} / 100</div>
        <div style="margin-top:8px;display:flex;flex-direction:column;gap:4px;">
          ${scoreRows}
        </div>
        <div class="btn" style="margin-top:20px;" id="restartBtn">重 新 制 陶</div>
      </div>
    `;
    document.getElementById('restartBtn').addEventListener('pointerdown', () => {
      ui.innerHTML = '';
      onRestart();
    });
  }
};
```

- [ ] **Step 2: 验证** — 刷新页面，应看到主菜单，点击器型后应显示"练泥"导语然后进入游戏循环（当前为空白 Canvas）。

---

### Task 5: 阶段 ① 练泥 —「揉泥去浊」

**Files:**
- Modify: `js/stages/knead.js`

**Produces:** 完整的练泥环节：旋转泥团、冒出气泡、点击戳破、命中率评分。

- [ ] **Step 1: 实现 knead.js**

```js
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};

TaoHun.Stages.Knead = {
  init(ctx, canvas) {
    this.ctx = ctx; this.canvas = canvas;
  },

  start() {
    this.duration = 18;         // 总时长秒
    this.elapsed = 0;
    this.bubbles = [];
    this.hitCount = 0;
    this.missCount = 0;
    this.combo = 0;
    this.maxCombo = 0;
    this.spawnTimer = 0;
    this.spawnInterval = 0.8;   // 初始生成间隔
    this.rotation = 0;
    this.mudScale = 1;
    this.completed = false;
  },

  getCenter() {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    return { x: w / 2, y: h / 2 };
  },

  update(dt) {
    if (this.completed) return;
    this.elapsed += dt;
    if (this.elapsed >= this.duration) { this.completed = true; return; }

    // 难度递增：间隔从 0.8s → 0.35s
    const progress = this.elapsed / this.duration;
    this.spawnInterval = 0.8 - progress * 0.45;

    // 旋转
    this.rotation += dt * (2 + progress * 3); // 越来越快

    // 泥团脉动
    this.mudScale = 1 + Math.sin(this.elapsed * 4) * 0.02;

    // 生成气泡
    this.spawnTimer += dt;
    if (this.spawnTimer >= this.spawnInterval) {
      this.spawnTimer -= this.spawnInterval;
      const center = this.getCenter();
      const angle = Math.random() * Math.PI * 2;
      const dist = 25 + Math.random() * 45;
      this.bubbles.push({
        x: center.x + Math.cos(angle) * dist,
        y: center.y + Math.sin(angle) * dist,
        radius: 6 + Math.random() * 10,
        life: 0.6 + Math.random() * 0.6,
        maxLife: 0,
        opacity: 1,
      });
      this.bubbles.forEach(b => { if (!b.maxLife) b.maxLife = b.life; });
    }

    // 气泡衰减
    for (const b of this.bubbles) {
      b.life -= dt;
      b.opacity = Math.max(0, b.life / b.maxLife);
    }

    // 清理过期气泡（miss）
    const expired = this.bubbles.filter(b => b.life <= 0);
    for (const b of expired) {
      this.missCount++;
      this.combo = 0;
      TaoHun.Audio.play('miss');
      TaoHun.Particles.emit({
        x: b.x, y: b.y, type: 'bubble', count: 5,
        spread: 2, speed: 60, life: 0.3, size: b.radius * 0.6, color: '#ffffff'
      });
    }
    this.bubbles = this.bubbles.filter(b => b.life > 0);

    TaoHun.Particles.update(dt);
  },

  render(ctx) {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const center = this.getCenter();

    // 背景 — 木案板
    ctx.fillStyle = '#2a1808';
    ctx.fillRect(0, 0, w, h);
    // 木纹
    for (let i = 0; i < 8; i++) {
      ctx.strokeStyle = `rgba(139,90,48,${0.08 + i * 0.01})`;
      ctx.lineWidth = 1;
      ctx.beginPath();
      ctx.moveTo(0, 80 + i * 70 + Math.sin(i * 2.3) * 30);
      for (let x = 0; x < w; x += 40) {
        ctx.lineTo(x, 80 + i * 70 + Math.sin(x * 0.003 + i) * 20);
      }
      ctx.stroke();
    }

    // 陶泥团 — 旋转脉动
    ctx.save();
    ctx.translate(center.x, center.y);
    ctx.rotate(this.rotation * 0.3);
    ctx.scale(this.mudScale, this.mudScale);

    // 泥团主体
    const mudGrad = ctx.createRadialGradient(0, -5, 10, 0, 0, 70);
    mudGrad.addColorStop(0, '#c49470');
    mudGrad.addColorStop(0.5, '#a06838');
    mudGrad.addColorStop(0.85, '#7a4a28');
    mudGrad.addColorStop(1, '#5a3018');
    ctx.fillStyle = mudGrad;
    ctx.beginPath();
    ctx.arc(0, 0, 70, 0, Math.PI * 2);
    ctx.fill();

    // 湿润光泽
    const shineGrad = ctx.createRadialGradient(-18, -22, 5, 0, 0, 70);
    shineGrad.addColorStop(0, 'rgba(255,240,220,0.18)');
    shineGrad.addColorStop(0.4, 'rgba(255,220,200,0.06)');
    shineGrad.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.fillStyle = shineGrad;
    ctx.beginPath();
    ctx.arc(0, 0, 70, 0, Math.PI * 2);
    ctx.fill();

    // 揉泥纹理
    for (let i = 0; i < 3; i++) {
      ctx.strokeStyle = `rgba(90,40,20,${0.15 + i * 0.05})`;
      ctx.lineWidth = 1.5;
      ctx.beginPath();
      const r = 45 + i * 10;
      for (let a = 0; a < Math.PI * 2; a += 0.1) {
        const rr = r + Math.sin(a * 5 + this.rotation * (1 + i * 0.5)) * 4;
        ctx.lineTo(Math.cos(a) * rr, Math.sin(a) * rr);
      }
      ctx.closePath();
      ctx.stroke();
    }
    ctx.restore();

    // 气泡
    for (const b of this.bubbles) {
      ctx.save();
      ctx.globalAlpha = b.opacity;
      const bGrad = ctx.createRadialGradient(b.x - 1, b.y - 1, b.radius * 0.2, b.x, b.y, b.radius);
      bGrad.addColorStop(0, 'rgba(255,255,255,0.7)');
      bGrad.addColorStop(0.5, 'rgba(220,200,180,0.3)');
      bGrad.addColorStop(1, 'rgba(200,180,150,0)');
      ctx.fillStyle = bGrad;
      ctx.beginPath();
      ctx.arc(b.x, b.y, b.radius, 0, Math.PI * 2);
      ctx.fill();
      // 高光点
      ctx.fillStyle = 'rgba(255,255,255,0.5)';
      ctx.beginPath();
      ctx.arc(b.x - b.radius * 0.25, b.y - b.radius * 0.25, b.radius * 0.25, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }

    // 粒子
    TaoHun.Particles.render(ctx);

    // 倒计时指示器
    const remaining = Math.max(0, this.duration - this.elapsed);
    ctx.fillStyle = '#e8c8a0';
    ctx.font = '14px "PingFang SC","Microsoft YaHei",sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText(`${remaining.toFixed(1)}s`, center.x, center.y + 100);

    // 连击提示
    if (this.combo >= 3) {
      ctx.fillStyle = '#ff9940';
      ctx.font = 'bold 18px "PingFang SC","Microsoft YaHei",sans-serif';
      ctx.fillText(`${this.combo} 连击!`, center.x, center.y - 100);
    }
  },

  handleInput(e) {
    if (this.completed) return;
    if (e.type !== 'pointerdown') return;

    const rect = this.canvas.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;

    // 检测命中
    let hit = null;
    for (const b of this.bubbles) {
      const dx = x - b.x, dy = y - b.y;
      if (Math.sqrt(dx * dx + dy * dy) < b.radius + 8) {
        hit = b; break;
      }
    }
    if (hit) {
      this.hitCount++;
      this.combo++;
      if (this.combo > this.maxCombo) this.maxCombo = this.combo;
      const isPerfect = hit.life / hit.maxLife < 0.3; // 快要消失前戳中 = perfect
      TaoHun.Audio.play(isPerfect ? 'perfect' : 'pop');
      TaoHun.Particles.emit({
        x: hit.x, y: hit.y, type: 'bubble', count: 10,
        spread: 3, speed: 100, life: 0.4, size: hit.radius * 0.4, color: '#ffffff'
      });
      this.bubbles = this.bubbles.filter(b => b !== hit);
    } else {
      this.combo = 0;
    }
  },

  isComplete() { return this.completed; },

  getScore() {
    const total = this.hitCount + this.missCount || 1;
    const hitRate = this.hitCount / total;
    const comboBonus = Math.min(1, this.maxCombo / 15);
    const score = Math.round(hitRate * 70 + comboBonus * 30);
    return {
      score: Math.min(100, score),
      details: { hitCount: this.hitCount, missCount: this.missCount, maxCombo: this.maxCombo }
    };
  },

  cleanup() { TaoHun.Particles.clear(); }
};
```

- [ ] **Step 2: 连接主控** — 在 `main.js` 中，确保 `startStage()` 正确使用 `TaoHun.Stages.Knead`，并在 `update()` 中调用 `this.currentInstance.isComplete()` 检查环节结束，如果结束则调用 `this.onStageComplete()`。

修改 `main.js` 的 `update` 逻辑（在 `loop` 方法中）：
```js
if (this.state === 'playing' && this.currentInstance) {
  this.currentInstance.update(dt);
  if (this.currentInstance.isComplete()) {
    this.onStageComplete();
  }
}
```

- [ ] **Step 3: 验证** — 完整走通 练泥 环节：菜单 → 选器型 → 导语 → 泥团气泡出现 → 点击戳破 → 18秒后自动结束。

---

### Task 6: 阶段 ② 拉坯 —「轮转成形」

**Files:**
- Modify: `js/stages/throw.js`

**Produces:** 拉坯环节：旋转陶轮、波动窗口、提拉/下压时机判定。

- [ ] **Step 1: 实现 throw.js**

```js
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};

TaoHun.Stages.Throw = {
  init(ctx, canvas) { this.ctx = ctx; this.canvas = canvas; },

  start() {
    this.duration = 22;
    this.elapsed = 0;
    this.wheelRotation = 0;
    this.vesselHeight = 30;     // 当前器壁高度（%）
    this.targetHeight = 30;
    this.vesselWidth = 40;      // 当前口径（%）
    this.targetWidth = 40;
    // 时机判定
    this.wavePhase = 0;
    this.waveSpeed = 1.5;
    this.actionZone = null;     // 'up' | 'down' | null
    this.zoneTimer = 0;
    this.zoneDuration = 0.6;
    // 评分
    this.perfectCount = 0;
    this.goodCount = 0;
    this.missCount = 0;
    this.completed = false;
    // 视觉
    this.handsY = 0;
  },

  update(dt) {
    if (this.completed) return;
    this.elapsed += dt;
    if (this.elapsed >= this.duration) { this.completed = true; return; }

    const progress = this.elapsed / this.duration;

    // 轮盘旋转
    this.wheelRotation += dt * (3 + progress * 4);

    // 波动
    this.waveSpeed = 1.5 + progress * 2;
    this.wavePhase += dt * this.waveSpeed;
    this.zoneTimer -= dt;

    // 生成新的动作区
    if (this.zoneTimer <= 0) {
      this.actionZone = Math.random() < 0.5 ? 'up' : 'down';
      this.zoneDuration = 0.55 - progress * 0.25; // 窗口越来越窄
      this.zoneTimer = this.zoneDuration + 0.3;   // 间隔
      // 目标变化
      if (this.actionZone === 'up') this.targetHeight = Math.min(90, this.targetHeight + 8 + Math.random() * 10);
      else this.targetWidth = Math.min(70, this.targetWidth + 5 + Math.random() * 8);
    }

    // 高度/宽度趋向目标（惯性）
    this.vesselHeight += (this.targetHeight - this.vesselHeight) * dt * 3;
    this.vesselWidth += (this.targetWidth - this.vesselWidth) * dt * 3;

    TaoHun.Particles.update(dt);
  },

  render(ctx) {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2, cy = h / 2;

    // 背景
    ctx.fillStyle = '#1a0c04'; ctx.fillRect(0, 0, w, h);

    // 陶轮底座
    const wheelR = 110;
    // 木轮
    ctx.save();
    ctx.translate(cx, cy + 60);
    ctx.rotate(this.wheelRotation);
    const wheelGrad = ctx.createRadialGradient(0, 0, 30, 0, 0, wheelR);
    wheelGrad.addColorStop(0, '#6b4a2a'); wheelGrad.addColorStop(0.7, '#4a2a10'); wheelGrad.addColorStop(1, '#2a1808');
    ctx.fillStyle = wheelGrad;
    ctx.beginPath(); ctx.arc(0, 0, wheelR, 0, Math.PI * 2); ctx.fill();
    // 木纹环
    for (let i = 0; i < 4; i++) {
      ctx.strokeStyle = `rgba(90,50,20,0.3)`; ctx.lineWidth = 1;
      ctx.beginPath(); ctx.arc(0, 0, 40 + i * 18, 0, Math.PI * 2); ctx.stroke();
    }
    ctx.restore();

    // 坯体
    const vesselBaseY = cy + 60;
    const vesselH = 40 + this.vesselHeight * 1.2;
    const vesselW = 30 + this.vesselWidth * 1.0;
    const vesselTop = vesselBaseY - vesselH;

    // 器身（剖面轮廓）
    ctx.save();
    // 外壁
    ctx.fillStyle = '#a06838';
    ctx.beginPath();
    ctx.moveTo(cx - vesselW * 0.9, vesselBaseY);
    ctx.quadraticCurveTo(cx - vesselW, vesselBaseY - vesselH * 0.5, cx - vesselW * 0.6, vesselTop);
    ctx.lineTo(cx + vesselW * 0.6, vesselTop);
    ctx.quadraticCurveTo(cx + vesselW, vesselBaseY - vesselH * 0.5, cx + vesselW * 0.9, vesselBaseY);
    ctx.closePath();
    ctx.fill();

    // 光泽
    const bodyGrad = ctx.createLinearGradient(cx - vesselW, 0, cx + vesselW, 0);
    bodyGrad.addColorStop(0, 'rgba(0,0,0,0.15)');
    bodyGrad.addColorStop(0.3, 'rgba(255,220,180,0.08)');
    bodyGrad.addColorStop(0.5, 'rgba(255,240,220,0.15)');
    bodyGrad.addColorStop(0.7, 'rgba(255,220,180,0.05)');
    bodyGrad.addColorStop(1, 'rgba(0,0,0,0.2)');
    ctx.fillStyle = bodyGrad;
    ctx.fill();

    // 器口椭圆
    ctx.fillStyle = '#8b5030';
    ctx.beginPath();
    ctx.ellipse(cx, vesselTop, vesselW * 0.6, 8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = '#6b3a1a';
    ctx.beginPath();
    ctx.ellipse(cx, vesselTop, vesselW * 0.6 - 6, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();

    // 手部轮廓
    ctx.fillStyle = '#c49470';
    const handY = vesselBaseY - vesselH * 0.5 + this.handsY;
    ctx.beginPath(); ctx.ellipse(cx - vesselW - 15, handY, 12, 22, -0.3, 0, Math.PI * 2); ctx.fill();
    ctx.beginPath(); ctx.ellipse(cx + vesselW + 15, handY, 12, 22, 0.3, 0, Math.PI * 2); ctx.fill();

    // 动作提示区
    if (this.actionZone && this.zoneTimer > this.zoneDuration * 0.3) {
      const zoneAlpha = Math.min(1, this.zoneTimer / this.zoneDuration);
      ctx.save();
      ctx.globalAlpha = zoneAlpha * 0.6;
      ctx.fillStyle = this.actionZone === 'up' ? '#4af' : '#f84';
      ctx.font = 'bold 24px "PingFang SC","Microsoft YaHei",sans-serif';
      ctx.textAlign = 'center';
      const label = this.actionZone === 'up' ? '↑ 提拉' : '↓ 下压';
      ctx.fillText(label, cx, vesselTop - 40);
      // 闪烁环
      ctx.strokeStyle = this.actionZone === 'up' ? '#4af' : '#f84';
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.ellipse(cx, vesselTop, vesselW * 0.6 + 12, 16, 0, 0, Math.PI * 2);
      ctx.stroke();
      ctx.restore();
    }

    // 按钮提示
    ctx.fillStyle = 'rgba(255,255,255,0.6)'; ctx.font = '12px sans-serif'; ctx.textAlign = 'center';
    ctx.fillText('↑ 方向键上 / ↓ 方向键下', cx, h - 30);

    // 粒子
    TaoHun.Particles.render(ctx);
  },

  handleInput(e) {
    if (this.completed) return;
    let action = null;
    if (e.type === 'keydown') {
      if (e.key === 'ArrowUp' || e.key === 'w' || e.key === 'W') action = 'up';
      if (e.key === 'ArrowDown' || e.key === 's' || e.key === 'S') action = 'down';
    }
    if (!action) return;
    if (e.repeat) return; // 忽略重复按键

    if (this.actionZone && this.zoneTimer > this.zoneDuration * 0.3) {
      if (action === this.actionZone) {
        // 判定精度
        const remaining = this.zoneTimer / this.zoneDuration;
        if (remaining > 0.7) { this.perfectCount++; TaoHun.Audio.play('perfect'); }
        else { this.goodCount++; TaoHun.Audio.play('good'); }
        // 粒子
        const w = this.canvas.width / devicePixelRatio;
        const h = this.canvas.height / devicePixelRatio;
        TaoHun.Particles.emit({
          x: w / 2, y: h / 2 - 40, type: 'chip', count: 8,
          spread: 2, speed: 80, life: 0.5, size: 3, color: '#a06838'
        });
        this.zoneTimer = 0;
        this.actionZone = null;
      } else {
        this.missCount++; TaoHun.Audio.play('miss');
        this.zoneTimer = 0;
        this.actionZone = null;
      }
    }
  },

  isComplete() { return this.completed; },

  getScore() {
    const total = this.perfectCount + this.goodCount + this.missCount || 1;
    const score = Math.round((this.perfectCount * 100 + this.goodCount * 60) / (total * 100) * 100);
    return { score: Math.min(100, score), details: { perfect: this.perfectCount, good: this.goodCount, miss: this.missCount } };
  },

  cleanup() { TaoHun.Particles.clear(); }
};
```

- [ ] **Step 2: 验证** — 练泥结束后自动进入拉坯，看到旋转陶轮和动作提示，按上下方向键响应。

---

### Task 7: 阶段 ③ 修坯 —「刀走泥落」

**Files:**
- Modify: `js/stages/trim.js`

**Produces:** 修坯环节：旋转坯体上的标记区域、点击/空格削切。

- [ ] **Step 1: 实现 trim.js**

```js
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};

TaoHun.Stages.Trim = {
  init(ctx, canvas) { this.ctx = ctx; this.canvas = canvas; },

  start() {
    this.duration = 18;
    this.elapsed = 0;
    this.rotation = 0;
    this.rotationSpeed = 2;
    // 多余区域
    this.zones = [];
    this.zoneTimer = 0;
    // 刀具
    this.bladeAngle = -0.8; // 刀的角度
    this.bladeTarget = -0.8;
    // 评分
    this.perfectCuts = 0;
    this.goodCuts = 0;
    this.badCuts = 0;
    this.completed = false;
    this.isCutting = false;
  },

  _spawnZone() {
    this.zones.push({
      angle: Math.random() * Math.PI * 2,
      size: 0.15 + Math.random() * 0.25, // 弧度
      depth: 1, // 待削深度
      life: 4,
    });
  },

  update(dt) {
    if (this.completed) return;
    this.elapsed += dt;
    if (this.elapsed >= this.duration) { this.completed = true; return; }

    const progress = this.elapsed / this.duration;
    this.rotation += dt * (2 + progress * 3);
    this.rotationSpeed = 2 + progress * 3;

    // 生成多余区域
    this.zoneTimer -= dt;
    if (this.zoneTimer <= 0) {
      this._spawnZone();
      this.zoneTimer = 0.8 - progress * 0.3;
    }

    // 区域衰减
    for (const z of this.zones) z.life -= dt;
    this.zones = this.zones.filter(z => z.life > 0);

    // 刀动画
    this.bladeAngle += (this.bladeTarget - this.bladeAngle) * dt * 8;

    TaoHun.Particles.update(dt);
  },

  render(ctx) {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2, cy = h / 2;

    ctx.fillStyle = '#1a0c04'; ctx.fillRect(0, 0, w, h);

    // 坯体（旋转）
    const vesselR = 80;
    ctx.save();
    ctx.translate(cx, cy);

    // 器身
    const bodyGrad = ctx.createRadialGradient(-10, -10, 20, 0, 0, vesselR);
    bodyGrad.addColorStop(0, '#c49470'); bodyGrad.addColorStop(0.6, '#8b5a30'); bodyGrad.addColorStop(1, '#6b3a1a');
    ctx.fillStyle = bodyGrad;
    ctx.beginPath(); ctx.arc(0, 0, vesselR, 0, Math.PI * 2); ctx.fill();
    // 器口
    ctx.fillStyle = '#5a3018';
    ctx.beginPath(); ctx.arc(0, 0, vesselR * 0.55, 0, Math.PI * 2); ctx.fill();

    // 多余区域标记（旋转中）
    for (const z of this.zones) {
      const a = z.angle + this.rotation;
      const alpha = Math.min(1, z.life / 1);
      ctx.save();
      ctx.globalAlpha = alpha * 0.5;
      ctx.fillStyle = '#ff4444';
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.arc(0, 0, vesselR, a - z.size / 2, a + z.size / 2);
      ctx.closePath();
      ctx.fill();
      // 边框
      ctx.strokeStyle = '#ff6666'; ctx.lineWidth = 2;
      ctx.stroke();
      ctx.restore();
    }

    ctx.restore();

    // 修坯刀
    const bladeX = cx + Math.cos(this.bladeAngle) * vesselR * 1.3;
    const bladeY = cy + Math.sin(this.bladeAngle) * vesselR * 1.3;
    ctx.save();
    ctx.translate(bladeX, bladeY);
    ctx.rotate(this.bladeAngle + Math.PI / 2);
    // 刀柄
    ctx.fillStyle = '#4a3020';
    ctx.fillRect(-4, -30, 8, 60);
    // 刀身
    ctx.fillStyle = '#888';
    const bladeGrad = ctx.createLinearGradient(-10, 0, 10, 0);
    bladeGrad.addColorStop(0, '#999'); bladeGrad.addColorStop(0.5, '#ddd'); bladeGrad.addColorStop(1, '#888');
    ctx.fillStyle = bladeGrad;
    ctx.beginPath();
    ctx.moveTo(-6, -30); ctx.lineTo(6, -30);
    ctx.lineTo(3, -60); ctx.lineTo(-3, -60);
    ctx.closePath();
    ctx.fill();
    ctx.restore();

    // 提示
    ctx.fillStyle = 'rgba(255,255,255,0.5)'; ctx.font = '12px sans-serif'; ctx.textAlign = 'center';
    ctx.fillText('点击 / 空格键 削切', cx, h - 30);

    TaoHun.Particles.render(ctx);
  },

  handleInput(e) {
    if (this.completed) return;
    const isCut = (e.type === 'pointerdown') || (e.type === 'keydown' && e.key === ' ');
    if (!isCut || e.repeat) return;

    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2, cy = h / 2;

    // 检查是否有区域在刀的角度附近
    let bestZone = null;
    let bestDist = Infinity;
    for (const z of this.zones) {
      const zoneAngle = (z.angle + this.rotation) % (Math.PI * 2);
      const bladeNorm = (this.bladeAngle + Math.PI * 2) % (Math.PI * 2);
      let diff = Math.abs(zoneAngle - bladeNorm);
      if (diff > Math.PI) diff = Math.PI * 2 - diff;
      if (diff < z.size * 0.7 && diff < bestDist) {
        bestDist = diff; bestZone = z;
      }
    }

    if (bestZone) {
      const precision = 1 - bestDist / (bestZone.size * 0.7);
      if (precision > 0.8) { this.perfectCuts++; TaoHun.Audio.play('perfect'); }
      else if (precision > 0.4) { this.goodCuts++; TaoHun.Audio.play('good'); }
      else { this.goodCuts++; TaoHun.Audio.play('scrape'); }

      TaoHun.Particles.emit({
        x: cx + Math.cos(this.bladeAngle) * 80,
        y: cy + Math.sin(this.bladeAngle) * 80,
        type: 'chip', count: 12, spread: 2, speed: 120, life: 0.6, size: 3, color: '#a06838'
      });
      this.zones = this.zones.filter(z => z !== bestZone);
      this.bladeTarget = this.bladeAngle + 0.15; // 刀回落
    } else {
      this.badCuts++; TaoHun.Audio.play('miss');
      this.bladeTarget = this.bladeAngle + 0.15;
    }
  },

  isComplete() { return this.completed; },

  getScore() {
    const total = this.perfectCuts + this.goodCuts + this.badCuts || 1;
    const score = Math.round((this.perfectCuts * 100 + this.goodCuts * 60) / (total * 100) * 100);
    return { score: Math.min(100, score), details: { perfect: this.perfectCuts, good: this.goodCuts, bad: this.badCuts } };
  },

  cleanup() { TaoHun.Particles.clear(); }
};
```

- [ ] **Step 2: 验证** — 拉坯→修坯自动过渡，红色标记区域出现，点击/空格削切有反馈。

---

### Task 8: 阶段 ④ 刻花 —「铁笔生花」

**Files:**
- Modify: `js/stages/carve.js`

**Produces:** 刻花环节：预设纹样轨迹、鼠标/触屏跟随刻画。

- [ ] **Step 1: 实现 carve.js**

```js
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};

TaoHun.Stages.Carve = {
  init(ctx, canvas) { this.ctx = ctx; this.canvas = canvas; },

  start() {
    this.duration = 25;
    this.elapsed = 0;
    // 在坯体表面生成纹样路径（缠枝纹简化版）
    this.pattern = this._generatePattern();
    this.pathProgress = 0;
    this.pathLength = this._computePathLength(this.pattern);
    this.isDrawing = false;
    this.pointerOnPath = false;
    // 评分
    this.onPathTime = 0;
    this.offPathTime = 0;
    this.totalDrawTime = 0;
    this.completed = false;
    // 刻画痕迹
    this.carveTrail = [];
  },

  _generatePattern() {
    // 在矩形区域生成简化缠枝纹路径
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2, cy = h / 2;
    const path = [];
    // 主枝：S形曲线从下到上
    const steps = 60;
    for (let i = 0; i <= steps; i++) {
      const t = i / steps;
      const x = cx + Math.sin(t * Math.PI * 3) * 90;
      const y = cy + 90 - t * 180;
      path.push({ x, y });
    }
    // 左侧分枝
    for (let i = 0; i <= 20; i++) {
      const t = i / 20;
      const baseIdx = Math.floor((0.3 + t * 0.4) * steps);
      const base = path[Math.min(baseIdx, steps)];
      const x = base.x - t * 60 + Math.sin(t * Math.PI) * 20;
      const y = base.y + t * 10;
      path.push({ x, y });
    }
    return path;
  },

  _computePathLength(pattern) {
    let len = 0;
    for (let i = 1; i < pattern.length; i++) {
      const dx = pattern[i].x - pattern[i-1].x;
      const dy = pattern[i].y - pattern[i-1].y;
      len += Math.sqrt(dx * dx + dy * dy);
    }
    return len;
  },

  _getClosestPoint(px, py) {
    let minDist = Infinity, minIdx = 0;
    for (let i = 0; i < this.pattern.length; i++) {
      const dx = px - this.pattern[i].x;
      const dy = py - this.pattern[i].y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < minDist) { minDist = dist; minIdx = i; }
    }
    return { index: minIdx, dist: minDist };
  },

  update(dt) {
    if (this.completed) return;
    this.elapsed += dt;
    if (this.elapsed >= this.duration) { this.completed = true; return; }

    if (this.isDrawing) {
      this.totalDrawTime += dt;
      if (this.pointerOnPath) {
        this.onPathTime += dt;
        this.pathProgress = Math.min(1, this.pathProgress + dt / 18);
        // 记录痕迹
        if (this._lastPointer) {
          this.carveTrail.push({ x: this._lastPointer.x, y: this._lastPointer.y, life: 1.5 });
        }
      } else {
        this.offPathTime += dt;
      }
    }

    // 痕迹衰减
    for (const t of this.carveTrail) t.life -= dt;
    this.carveTrail = this.carveTrail.filter(t => t.life > 0);

    TaoHun.Particles.update(dt);
  },

  render(ctx) {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2, cy = h / 2;

    ctx.fillStyle = '#1a0c04'; ctx.fillRect(0, 0, w, h);

    // 坯体作为背景
    const bodyGrad = ctx.createRadialGradient(cx, cy, 30, cx, cy, 120);
    bodyGrad.addColorStop(0, '#c49470'); bodyGrad.addColorStop(0.5, '#a06838'); bodyGrad.addColorStop(0.9, '#6b3a1a'); bodyGrad.addColorStop(1, '#1a0c04');
    ctx.fillStyle = bodyGrad;
    ctx.beginPath(); ctx.arc(cx, cy, 120, 0, Math.PI * 2); ctx.fill();

    // 纹样路径（未完成部分）
    const doneCount = Math.floor(this.pathProgress * this.pattern.length);
    // 已完成路径 — 金色刻痕
    if (doneCount > 0) {
      ctx.strokeStyle = 'rgba(220,180,100,0.8)';
      ctx.lineWidth = 2.5;
      ctx.beginPath();
      ctx.moveTo(this.pattern[0].x, this.pattern[0].y);
      for (let i = 1; i < doneCount; i++) {
        ctx.lineTo(this.pattern[i].x, this.pattern[i].y);
      }
      ctx.stroke();
      // 光晕
      ctx.strokeStyle = 'rgba(255,200,120,0.3)';
      ctx.lineWidth = 6;
      ctx.stroke();
    }

    // 未完成路径 — 灰色引导线
    if (doneCount < this.pattern.length - 1) {
      ctx.setLineDash([4, 6]);
      ctx.strokeStyle = 'rgba(255,255,255,0.2)';
      ctx.lineWidth = 1.5;
      ctx.beginPath();
      ctx.moveTo(this.pattern[Math.max(0, doneCount)].x, this.pattern[Math.max(0, doneCount)].y);
      for (let i = Math.max(1, doneCount); i < this.pattern.length; i++) {
        ctx.lineTo(this.pattern[i].x, this.pattern[i].y);
      }
      ctx.stroke();
      ctx.setLineDash([]);
    }

    // 当前指针
    if (this.isDrawing && this._lastPointer) {
      ctx.fillStyle = '#ff9940';
      ctx.beginPath();
      ctx.arc(this._lastPointer.x, this._lastPointer.y, 5, 0, Math.PI * 2);
      ctx.fill();
    }

    // 偏离警告
    if (this.isDrawing && !this.pointerOnPath) {
      ctx.fillStyle = 'rgba(255,60,30,0.6)';
      ctx.font = '16px "PingFang SC","Microsoft YaHei",sans-serif';
      ctx.textAlign = 'center';
      ctx.fillText('偏离纹样!', cx, cy - 140);
    }

    // 提示
    ctx.fillStyle = 'rgba(255,255,255,0.5)'; ctx.font = '12px sans-serif'; ctx.textAlign = 'center';
    ctx.fillText('按住鼠标/手指沿纹样描画', cx, h - 30);

    TaoHun.Particles.render(ctx);
  },

  handleInput(e) {
    if (this.completed) return;
    const rect = this.canvas.getBoundingClientRect();
    const x = (e.clientX || (e.touches && e.touches[0]?.clientX) || 0) - rect.left;
    const y = (e.clientY || (e.touches && e.touches[0]?.clientY) || 0) - rect.top;

    if (e.type === 'pointerdown') {
      this.isDrawing = true;
      this._lastPointer = { x, y };
      const closest = this._getClosestPoint(x, y);
      this.pointerOnPath = closest.dist < 30;
      if (this.pointerOnPath) TaoHun.Audio.play('scratch');
    } else if (e.type === 'pointermove' && this.isDrawing) {
      this._lastPointer = { x, y };
      const closest = this._getClosestPoint(x, y);
      this.pointerOnPath = closest.dist < 30;
    } else if (e.type === 'pointerup') {
      this.isDrawing = false;
      this._lastPointer = null;
    }
  },

  isComplete() { return this.completed; },

  getScore() {
    const total = this.onPathTime + this.offPathTime || 1;
    const accuracy = this.onPathTime / total;
    const completion = this.pathProgress;
    const score = Math.round(accuracy * 60 + completion * 40);
    return { score: Math.min(100, score), details: { accuracy, completion } };
  },

  cleanup() { TaoHun.Particles.clear(); }
};
```

- [ ] **Step 2: 验证** — 修坯→刻花过渡，在坯体上可见缠枝纹路径，按住鼠标沿路径描画。

---

### Task 9: 阶段 ⑤ 烧制 —「烈火成器」

**Files:**
- Modify: `js/stages/fire.js`

**Produces:** 烧制环节：窑炉温度控制、左右拖动/点击调节火力、温度区间判定。

- [ ] **Step 1: 实现 fire.js**

```js
window.TaoHun = window.TaoHun || {};
TaoHun.Stages = TaoHun.Stages || {};

TaoHun.Stages.Fire = {
  init(ctx, canvas) { this.ctx = ctx; this.canvas = canvas; },

  start() {
    this.duration = 28;
    this.elapsed = 0;
    this.temperature = 800;       // 当前温度
    this.targetZone = { min: 930, max: 980 }; // 最佳温度区间
    this.zonePhase = 0;
    this.fuelLevel = 0;           // 0~1 加柴力度
    // 评分
    this.inZoneTime = 0;
    this.totalTime = 0;
    this.burnedCount = 0;         // 温度过高次数
    this.coldCount = 0;           // 温度过低次数
    this.completed = false;
    // 粒子
    this.sparkTimer = 0;
  },

  update(dt) {
    if (this.completed) return;
    this.elapsed += dt;
    this.totalTime += dt;

    if (this.elapsed >= this.duration) { this.completed = true; return; }

    const progress = this.elapsed / this.duration;

    // 最佳区间移动
    this.zonePhase += dt * 0.25;
    const zoneShift = Math.sin(this.zonePhase) * 30;
    this.targetZone.min = 930 + zoneShift - progress * 15;
    this.targetZone.max = 980 + zoneShift - progress * 10;

    // 温度变化：燃料效果 + 自然冷却
    this.temperature += this.fuelLevel * 50 * dt; // 加柴升温
    this.temperature -= 10 * dt;                   // 自然散热
    this.temperature += (Math.random() - 0.5) * 15 * dt; // 随机波动

    // 燃料衰减
    this.fuelLevel = Math.max(0, this.fuelLevel - dt * 0.8);

    // 判定
    const inZone = this.temperature >= this.targetZone.min && this.temperature <= this.targetZone.max;
    if (inZone) this.inZoneTime += dt;

    if (this.temperature > this.targetZone.max + 30) this.burnedCount++;
    if (this.temperature < this.targetZone.min - 30) this.coldCount++;

    // 火星粒子
    this.sparkTimer -= dt;
    if (this.sparkTimer <= 0 && this.fuelLevel > 0.3) {
      const w = this.canvas.width / devicePixelRatio;
      const h = this.canvas.height / devicePixelRatio;
      TaoHun.Particles.emit({
        x: w / 2 + (Math.random() - 0.5) * 200,
        y: h * 0.55,
        type: 'spark', count: 3 + Math.floor(this.fuelLevel * 8),
        spread: 2, speed: 60 + this.fuelLevel * 100, life: 0.4 + Math.random() * 0.6,
        size: 2 + Math.random() * 3, color: ['#ff9940', '#ffcc66', '#ff6b35'][Math.floor(Math.random() * 3)]
      });
      this.sparkTimer = 0.08 - this.fuelLevel * 0.04;
    }

    TaoHun.Particles.update(dt);
  },

  render(ctx) {
    const w = this.canvas.width / devicePixelRatio;
    const h = this.canvas.height / devicePixelRatio;
    const cx = w / 2;

    // 窑炉背景
    ctx.fillStyle = '#1a0c04'; ctx.fillRect(0, 0, w, h);

    // 窑壁砖块纹理
    for (let row = 0; row < 8; row++) {
      for (let col = 0; col < 12; col++) {
        const bx = col * 70 + (row % 2) * 35;
        const by = row * 45 - 20;
        ctx.fillStyle = `rgba(60,30,15,${0.3 + Math.random() * 0.1})`;
        ctx.fillRect(bx, by, 65, 40);
        ctx.strokeStyle = 'rgba(40,20,10,0.5)';
        ctx.strokeRect(bx, by, 65, 40);
      }
    }

    // 火焰光芒背景
    const fireGlow = ctx.createRadialGradient(cx, h * 0.55, 20, cx, h * 0.55, 300);
    fireGlow.addColorStop(0, `rgba(255,150,40,${0.15 + this.fuelLevel * 0.25})`);
    fireGlow.addColorStop(0.5, `rgba(255,100,20,${0.05 + this.fuelLevel * 0.1})`);
    fireGlow.addColorStop(1, 'transparent');
    ctx.fillStyle = fireGlow;
    ctx.fillRect(0, 0, w, h);

    // 火焰（底部）
    const flameH = 30 + this.fuelLevel * 80;
    for (let i = 0; i < 5; i++) {
      const fx = cx - 60 + i * 30;
      const fy = h * 0.65;
      const grad = ctx.createLinearGradient(fx, fy, fx, fy - flameH * (0.6 + Math.random() * 0.4));
      grad.addColorStop(0, '#ff6b35');
      grad.addColorStop(0.4, '#ff9940');
      grad.addColorStop(0.8, '#ffcc66');
      grad.addColorStop(1, 'rgba(255,200,100,0)');
      ctx.fillStyle = grad;
      ctx.beginPath();
      ctx.moveTo(fx - 14, fy);
      ctx.quadraticCurveTo(fx - 5 + Math.sin(this.elapsed * 8 + i) * 8, fy - flameH * 0.5, fx, fy - flameH + Math.sin(this.elapsed * 10 + i) * 10);
      ctx.quadraticCurveTo(fx + 5 + Math.cos(this.elapsed * 7 + i) * 8, fy - flameH * 0.5, fx + 14, fy);
      ctx.fill();
    }

    // 温度计
    const thermoX = 60, thermoY = h * 0.15, thermoH = h * 0.55, thermoW = 20;
    // 管
    ctx.fillStyle = 'rgba(0,0,0,0.5)';
    ctx.fillRect(thermoX - 2, thermoY, thermoW + 4, thermoH);
    // 水银柱
    const tempRatio = Math.max(0, Math.min(1, (this.temperature - 700) / 400));
    const mercuryH = thermoH * tempRatio;
    const mercGrad = ctx.createLinearGradient(0, thermoY + thermoH - mercuryH, 0, thermoY + thermoH);
    mercGrad.addColorStop(0, '#ff4444'); mercGrad.addColorStop(1, '#ff9940');
    ctx.fillStyle = mercGrad;
    ctx.fillRect(thermoX, thermoY + thermoH - mercuryH, thermoW, mercuryH);

    // 最佳温度区间标记（温度计上）
    const zoneY1 = thermoY + thermoH - (this.targetZone.max - 700) / 400 * thermoH;
    const zoneY2 = thermoY + thermoH - (this.targetZone.min - 700) / 400 * thermoH;
    ctx.fillStyle = 'rgba(0,255,150,0.4)';
    ctx.fillRect(thermoX - 6, zoneY1, thermoW + 12, zoneY2 - zoneY1);
    ctx.strokeStyle = '#0f6'; ctx.lineWidth = 1;
    ctx.strokeRect(thermoX - 6, zoneY1, thermoW + 12, zoneY2 - zoneY1);

    // 温度数字
    ctx.fillStyle = '#e8c8a0'; ctx.font = 'bold 16px monospace'; ctx.textAlign = 'right';
    ctx.fillText(`${Math.round(this.temperature)}°C`, thermoX - 15, thermoY - 10);
    ctx.fillText(`目标 ${this.targetZone.min}-${this.targetZone.max}°C`, thermoX - 15, thermoY + thermoH + 20);

    // 坯体在窑中
    const potX = w * 0.7, potY = h * 0.35;
    // 温度颜色变化
    const heatRatio = Math.max(0, Math.min(1, (this.temperature - 850) / 150));
    const potColor = this._lerpColor('#a06838', '#e8c8a0', heatRatio);
    ctx.fillStyle = potColor;
    ctx.beginPath();
    ctx.arc(potX, potY, 35, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.arc(potX, potY + 15, 25, Math.PI, 0);
    ctx.fill();
    // 光泽
    const glazeAlpha = Math.max(0, heatRatio - 0.6) * 0.4;
    const glazeGrad = ctx.createRadialGradient(potX - 8, potY - 10, 5, potX, potY, 35);
    glazeGrad.addColorStop(0, `rgba(122,184,160,${glazeAlpha})`);
    glazeGrad.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.fillStyle = glazeGrad;
    ctx.beginPath(); ctx.arc(potX, potY, 35, 0, Math.PI * 2); ctx.fill();

    // 控制提示
    ctx.fillStyle = 'rgba(255,255,255,0.5)'; ctx.font = '12px sans-serif'; ctx.textAlign = 'center';
    ctx.fillText('← → 或点击左右区域 调节火力', cx, h - 20);
    ctx.fillText(`火力: ${'█'.repeat(Math.floor(this.fuelLevel * 10))}${'░'.repeat(10 - Math.floor(this.fuelLevel * 10))}`, cx, h - 40);

    TaoHun.Particles.render(ctx);
  },

  _lerpColor(c1, c2, t) {
    const r1 = parseInt(c1.slice(1,3), 16), g1 = parseInt(c1.slice(3,5), 16), b1 = parseInt(c1.slice(5,7), 16);
    const r2 = parseInt(c2.slice(1,3), 16), g2 = parseInt(c2.slice(3,5), 16), b2 = parseInt(c2.slice(5,7), 16);
    const r = Math.round(r1 + (r2 - r1) * t);
    const g = Math.round(g1 + (g2 - g1) * t);
    const b = Math.round(b1 + (b2 - b1) * t);
    return `rgb(${r},${g},${b})`;
  },

  handleInput(e) {
    if (this.completed) return;

    if (e.type === 'keydown') {
      if (e.key === 'ArrowLeft' || e.key === 'a') { this.fuelLevel = Math.min(1, this.fuelLevel + 0.3); TaoHun.Audio.play('fireCrackle'); }
      if (e.key === 'ArrowRight' || e.key === 'd') { this.fuelLevel = Math.max(0, this.fuelLevel - 0.3); }
    }

    if (e.type === 'pointerdown') {
      const rect = this.canvas.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const w = this.canvas.width / devicePixelRatio;
      if (x < w / 2) {
        this.fuelLevel = Math.min(1, this.fuelLevel + 0.3);
        TaoHun.Audio.play('fireCrackle');
      } else {
        this.fuelLevel = Math.max(0, this.fuelLevel - 0.3);
      }
    }
  },

  isComplete() { return this.completed; },

  getScore() {
    const ratio = this.totalTime > 0 ? this.inZoneTime / this.totalTime : 0;
    const penalty = Math.min(0.3, (this.burnedCount + this.coldCount) * 0.005);
    const score = Math.round(Math.max(0, ratio - penalty) * 100);
    return { score: Math.min(100, score), details: { inZoneRatio: ratio, burned: this.burnedCount, cold: this.coldCount } };
  },

  cleanup() { TaoHun.Particles.clear(); }
};
```

- [ ] **Step 2: 验证** — 刻花→烧制过渡，窑炉火焰、温度计、按←加柴→减柴调节温度。

---

### Task 10: 主控完善 — 流程串联 & Bug 修复

**Files:**
- Modify: `js/main.js`

**Produces:** 完整可玩的五环节游戏流程。

- [ ] **Step 1: 完善 main.js 流程**

确认以下逻辑完整：

```js
// 在 TaoHun.Game 中：

// 1. showStageIntro 完成后正确进入 playing 状态
// 2. 每帧检查 isComplete()
// 3. onStageComplete() 正确递增 currentStage
// 4. 所有 5 个环节结束后进入 showResult
// 5. showResult 的重新开始按钮正确回到 showMenu

// 修改 showStageIntro 调用（移除内部 callback 嵌套）：
startStage() {
  const key = this.stageKeys[this.currentStage];
  const StageCtor = TaoHun.Stages[key.charAt(0).toUpperCase() + key.slice(1)];
  this.currentInstance = Object.create(StageCtor);
  this.currentInstance.init(this.ctx, this.canvas);
  this.currentInstance.start();

  // 先显示导语，然后进入 playing
  this.state = 'intro';
  TaoHun.UI.showStageIntro(this.stageNames[this.currentStage], () => {
    this.state = 'playing';
  });
},
```

- [ ] **Step 2: 全流程测试** — 从菜单开始，完整走过 练泥→拉坯→修坯→刻花→烧制→结果画面→重新开始。

---

### Task 11: 视觉润色 — 粒子、动画、过渡效果

**Files:**
- Modify: `js/main.js`, `js/ui.js`, 各 stage 文件

**Produces:** 游戏画面精美，过渡流畅。

- [ ] **Step 1: 环节过渡时 Canvas 淡入淡出**

在 `main.js` 的 `render()` 中添加过渡 alpha：

```js
render() {
  const ctx = this.ctx;
  const w = this.canvas.width / devicePixelRatio;
  const h = this.canvas.height / devicePixelRatio;

  ctx.fillStyle = '#1a0c04';
  ctx.fillRect(0, 0, w, h);

  if ((this.state === 'playing' || this.state === 'intro') && this.currentInstance) {
    this.currentInstance.render(ctx);
  }

  if (this.state === 'playing') {
    TaoHun.UI.renderHUD(ctx, this.currentStage, this.scores[this.currentStage]);
  }

  // 全局粒子始终渲染
  TaoHun.Particles.render(ctx);
},
```

- [ ] **Step 2: 增强火焰粒子密度**

在 `fire.js` 中，始终有基础火星（不受 fuelLevel 限制）：
```js
// 基础环境火星
if (Math.random() < 0.3) {
  TaoHun.Particles.emit({
    x: w/2 + (Math.random() - 0.5) * 250,
    y: h * 0.55, type: 'spark', count: 2,
    spread: 3, speed: 40, life: 0.8, size: 2, color: '#ff9940'
  });
}
```

- [ ] **Step 3: 练泥气泡微调** — 增加气泡颜色变化，部分气泡带淡黄色（暗示泥中杂质）：
```js
bubbleColors = ['rgba(255,255,255,0.7)', 'rgba(255,250,240,0.6)', 'rgba(255,240,200,0.5)'];
```

- [ ] **Step 4: 拉坯成功反馈** — Perfect 判定时器壁上闪现金色光晕
```js
// 在 throw.js render() 中：
if (this._lastPerfectTime && this.elapsed - this._lastPerfectTime < 0.3) {
  const alpha = 1 - (this.elapsed - this._lastPerfectTime) / 0.3;
  ctx.strokeStyle = `rgba(255,200,80,${alpha * 0.6})`;
  ctx.lineWidth = 4;
  // 器壁光晕...
}
```

- [ ] **Step 5: 验证** — 全流程运行，视觉效果精致、过渡自然。

---

### Task 12: 移动端适配与最终测试

**Files:**
- Modify: `css/style.css`, `index.html`

**Produces:** 移动端可流畅游玩。

- [ ] **Step 1: 添加移动端适配 CSS**

```css
/* 移动端响应式 */
@media (max-width: 768px) {
  .game-title { font-size: 32px; letter-spacing: 4px; }
  .game-subtitle { font-size: 13px; letter-spacing: 3px; }
  .btn { padding: 10px 28px; font-size: 16px; }
  
  .shape-card {
    width: 70px !important; height: 70px !important;
    font-size: 16px !important;
  }
}
```

- [ ] **Step 2: 防止移动端误操作**

在 `index.html` 添加：
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<style>
  * { -webkit-tap-highlight-color: transparent; }
  body { touch-action: none; overscroll-behavior: none; }
</style>
```

- [ ] **Step 3: 触摸事件兼容检查**

确认所有 `handleInput` 方法都正确处理 `pointerdown`/`pointermove`/`pointerup`，以及 `e.clientX` 在移动端可用。

- [ ] **Step 4: 多浏览器测试** — Chrome、Edge、Firefox 各完整运行一次。

---

## 测试检查清单

- [ ] 菜单：四种器型可选择，点击后进入练泥
- [ ] 练泥：气泡在泥团上冒出，点击戳破有音效反馈，18秒结束
- [ ] 拉坯：陶轮旋转，提拉/下压提示出现，方向键操作有判定
- [ ] 修坯：红色标记区域出现，点击/空格削切有泥屑粒子
- [ ] 刻花：缠枝纹路径可见，按住鼠标沿路径描画
- [ ] 烧制：火焰粒子、温度计、加柴/减柴控制温度
- [ ] 结果：五项得分汇总，等第评定，重新开始按钮
- [ ] 全程无 console 报错
- [ ] 移动端触摸可操作
- [ ] 音效正常播放（需用户交互一次后）
